name: jrecord
description: Voice Recording App By Just Codes Developers


publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^0.16.2
  cupertino_icons: ^1.0.0
  flutter_audio_recorder: ^0.5.5
  path_provider: ^1.6.24
  fluttertoast: ^7.1.6
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
